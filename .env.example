# API
PORT=3000

# PostgreSQL connection (used by Prisma)
# For docker-compose, use the DATABASE_URL from docker-compose.yml
DATABASE_URL=postgresql://postgres:postgres@localhost:5432/dsalta?schema=public
